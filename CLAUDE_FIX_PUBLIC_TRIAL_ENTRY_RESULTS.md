# CLAUDE_FIX_PUBLIC_TRIAL_ENTRY_RESULTS

> Ответ на `CLAUDE_FIX_PUBLIC_TRIAL_ENTRY.md`. Claude, 2026-07-04. Оба фикса готовы.

## 1. Бренд на входном экране — починен

Корень: заголовок входного экрана («Мужская Эстетика» + «Имя · Город»),
Wordmark и alt-тексты были зашиты строками МИМО `APP_DATA` — boot менял
данные, а эти места их не читали. Теперь читают:
`(window.APP_DATA…brand.name || прежняя строка)` — 9 заякоренных замен в
обоих файлах, фолбэки байт-в-байт прежние (прод-рендер без конфига не
меняется). Плюс телефон/адрес в превью виджетов кастомайзера.

## 2. Trial-баннер на входном экране — есть

Экран «Выберите версию» при `tenant_status === 'trial'` &&
`client_registration_enabled === false` сразу показывает:
«Салон готовится к запуску — вход пока только для сотрудников» —
до любых попыток входа (подсказка на шаге телефона тоже осталась).

## Проверка по твоему критерию

Harness с конфигом `codex-ui-626085` (живой конфиг сверен: brand.name
«Codex UI Salon», trial, client_reg=false): brand.name применяется,
title салона, условие баннера истинно, booking_mode=preview. Parse-check
обоих файлов чист. Остальной safe-mode flow не тронут (замены — только
источники строк).

## Примечание

`tenantize_frontend.py` (прод-патчер white-label) обновлён: замены,
теперь живущие прямо в исходнике, распознаёт как применённые
(skip-if-already) — перегон на новом app.html прошёл чисто.
